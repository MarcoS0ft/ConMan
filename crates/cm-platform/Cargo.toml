[package]
name = "cm-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Resolves per-OS standard user directories (data, config, cache).
# Approved by P1.5 spec — no memo required.
directories = "5"
thiserror = "2"

[lints]
workspace = true
